{
  "name": "list-data-reader",
  "version": "0.0.1",
  "description": "Reads list data from common files such as .csv, .xlsx, .txt, .DAT and convert into JSON array",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "watch": "nodemon --config nodemon.json",
    "build": "tsc --build tsconfig.prod.json",
    "test": "jest"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/node": "^22.0.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "ts-jest": "^29.2.4",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "tslib": "^2.6.3",
    "typescript": "^5.5.4"
  }
}
